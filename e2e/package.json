{
  "name": "district-sale-e2e",
  "version": "0.0.0",
  "private": true,
  "description": "End-to-end test (Playwright) driving the Angular client against the running API + DB.",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1"
  }
}
